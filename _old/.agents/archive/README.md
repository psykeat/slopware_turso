# Archive

Historical documentation only.

- Do not load these files in the default Codex context path.
- Use only when you need prior decisions, old handoffs, or retired feature plans.
- Active documentation lives one level up in `.agents/` and `map.md`.
