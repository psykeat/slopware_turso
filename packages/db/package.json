{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "db": "drizzle-kit",
    "generate": "drizzle-kit generate && pnpm docs",
    "migrate": "drizzle-kit migrate && pnpm docs",
    "docs": "tsx src/scripts/generate-docs.ts",
    "auth:generate": "vpx https://pkg.pr.new/auth@9489 generate --config ../auth/src/auth.ts --y --output ./src/schema/auth.schema.ts"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "nanoid": "catalog:",
    "postgres": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "@tanstack/react-start": "catalog:"
  }
}
